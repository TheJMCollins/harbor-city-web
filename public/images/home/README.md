# Homepage images

Drop the following from `/Users/godfrey/Downloads/HCC-deploy/` into this folder:

- `pastors.jpg` — Pastor Michael & Lisa Collins photo (used in OurStory section)

The icon file `hcc-icon-blue.png` is already placed here (canonical Harbor City
mark, used in Nav).

When the vector version of the icon is ready, replace `hcc-icon-blue.png` with
an inline SVG inside `src/components/global/HarborCityIcon.astro`. The component
API (size, title, class props) stays the same — no other files need to change.
